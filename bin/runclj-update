#!/bin/bash
set -euo pipefail
filepath=$1
root=$(runclj-root $filepath)
src=$root/src
ns=$(runclj-meta $filepath :ns | tr '-' '_')
runclj-meta $filepath :macro > $src/$ns.clj
if [ "${release:-n}" = "y" ]; then
    runclj-meta $filepath :source | grep -v '(defonce repl (repl/connect' > $src/$ns.cljs
else
    runclj-meta $filepath :source > $src/$ns.cljs
fi
