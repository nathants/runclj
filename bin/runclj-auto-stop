#!/bin/bash
filepath=$1

for pid in $(runclj-auto-status $filepath | awk '{print $2}'); do
    if [ ! -z "$pid" ]; then
        pkill --signal 9 -P $pid &>/dev/null
        kill -9 $pid &>/dev/null
        echo killed pid: $pid
    fi
done
